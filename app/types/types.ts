export type User = {
    email:string,
    password:string
}

export type Product = {
    _id: 'string',
    name:string,
    batch:string,
    expireDate:string,
    employee:string,
    quantity:string
}